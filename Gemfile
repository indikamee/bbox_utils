source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '~> 3.3'

# Use Rails 7.0.6 or higher (but < 8.0)
gem 'rails', '~> 7.0.6'

# Use PostgreSQL as the database for Active Record
gem 'pg'

# Use Puma as the app server
gem 'puma', '~> 5.0'

# Use SCSS for stylesheets
gem 'sass-rails', '~> 6.0'

# Use Uglifier as compressor for JavaScript assets (optional, Rails 7 uses esbuild/jsbundling)
gem 'uglifier', '>= 4.2.0'

# Use CoffeeScript for .coffee assets and views (optional, not recommended for Rails 7)
# gem 'coffee-rails', '~> 5.0'


gem 'coffee-rails', '~> 5.0.0'

gem 'turbolinks', '~> 5'
gem 'jbuilder', '~> 2.5'

# Hotwire's SPA-like page accelerator [https://turbo.hotwired.dev]
gem "turbo-rails"
# Importmap for managing JavaScript modules
gem 'importmap-rails'
# Stimulus for building JavaScript controllers
gem 'stimulus-rails'

gem 'bootsnap', '>= 1.1.0', require: false

group :development, :test do
  gem 'byebug', platforms: [:mri, :mingw, :x64_mingw]
end

group :development do
  gem 'web-console', '>= 4.0.0'
  gem 'listen', '~> 3.7'
  #gem 'spring'
  #gem 'spring-watcher-listen', '~> 2.0.0'
end

group :test do
  # Add test gems as needed
end

gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]

gem 'bootstrap', '~> 5.3.2'

gem 'bootstrap-social-rails'
gem 'font-awesome-rails'

gem 'jquery-rails'
gem 'jquery-datatables-rails', git: 'https://github.com/rweng/jquery-datatables-rails.git'

gem "execjs", "2.8.1"

gem 'prawn'
gem 'prawn-table'

gem 'devise'
gem 'cancancan', '~> 3.0'
gem 'rolify'
gem 'omniauth', '>= 2.0'
gem 'omniauth-google-oauth2', '>= 1.0.0'
gem 'omniauth-rails_csrf_protection'  # Add this for CSRF protection

gem 'font-awesome-rails'

gem 'pretender'
gem "rqrcode"
gem "barby"
