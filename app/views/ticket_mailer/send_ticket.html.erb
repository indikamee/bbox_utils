    <h1> <%= @ticket.event.title %> </h1>
    Please get your ticket: 
    <table class="table table-striped">	
    <tr>
    <th>Event</th> 
    <td><%= link_to "#{@ticket.event.id} - #{@ticket.event.title}" , @ticket.event %></td>
    </tr>
    <tr>
    <th>TicketNo</th> 
    <td><%= "#{@ticket.ticket_no} ( #{@ticket.ticket_type} $ #{@ticket.price} )" %> </td>
    </tr>
    <tr>
    <th>State</th> 
    <td>State: '<%= @ticket.state %>'  Table:<%= @ticket.table_id%> </td>
    </tr>

    <tr>
    <th>Sold by</th> 
    <td><%= (link_to @ticket.agent.name, @ticket.agent) if @ticket.agent%></td>
    </tr>

    <tr>
    <th>Allocated to</th> 
    <td><%= (link_to @ticket.person.name, @ticket.person) if @ticket.person %></td>
    </tr>
    </table>
