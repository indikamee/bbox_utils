<style>
table, th, td {
  border: 1px solid black;
  padding: 15px;
}
</style>

Dear <%=@person.name%>,

<p>
Please get your tickets for `<%=@event.title %>` attached herewith.
</p>

<table border="1">
<tr>
    <th>ID</th>
    <th>Type</th>
    <th>State</th>
    <th>Price($)</th>
    <th>Sold By</th>
    <th>Allocated To</th>
</tr>
<% @tickets.each do |ticket| %>
    <tr>
        
        <td><%= link_to ticket.ticket_no, ticket %></td>
        <td><%= ticket.ticket_type %></td>
        <td><%= "#{ticket.state}" %> <%= "Table:#{ticket.table_id}" if ticket.table_id.present? %></td>
        <td><%= ticket.price %></td>
        <td><%= (link_to ticket.agent.name, ticket.agent) if ticket.agent %></td>
        <td><%= (link_to ticket.person.name, ticket.person) if ticket.person %></td>
      </tr>
<% end%>
</table>


<br/>
Best Regards,<br/>
<%=@event.title %> - Organizing committee <br/>
<a href="umaav.org.au">University of Moratuwa Alumni Association-Victoria (UMAAV)</a> <br/>
